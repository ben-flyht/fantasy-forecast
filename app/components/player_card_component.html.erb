<% if @player %>
  <div class="flex items-center space-x-2 md:space-x-4">
    <div class="w-8 h-8 md:w-16 md:h-16 flex-shrink-0">
      <% if @player.photo_url %>
        <img src="<%= @player.photo_url(size: '40x40') %>" alt="<%= @player.name %>" class="w-8 h-8 md:w-16 md:h-16 rounded-full object-cover" onerror="this.style.display='none'">
      <% end %>
    </div>
    <div class="flex flex-col min-w-0">
      <span class="text-xs text-gray-900 break-words"><%= @player.first_name %></span>
      <span class="text-sm md:text-lg font-bold text-gray-900 break-words"><%= @player.last_name %></span>
      <% if @player.team %>
        <span class="text-xs text-gray-700"><%= @player.team.name %></span>
      <% end %>
    </div>
  </div>
<% else %>
  <div class="flex items-center space-x-2 md:space-x-4">
    <div class="w-8 h-8 md:w-16 md:h-16 flex-shrink-0 bg-gray-200 rounded-full"></div>
    <div class="flex flex-col min-w-0">
      <span class="text-xs text-gray-400">Not selected</span>
    </div>
  </div>
<% end %>
