<svg viewBox="0 0 <%= SparklineComponent::WIDTH %> <%= SparklineComponent::HEIGHT %>"
     width="<%= SparklineComponent::WIDTH %>" height="<%= SparklineComponent::HEIGHT %>"
     class="overflow-visible" fill="none" aria-hidden="true" focusable="false">
  <polyline points="<%= points %>" stroke="<%= stroke %>" stroke-width="1.5"
            stroke-linecap="round" stroke-linejoin="round" />
</svg>
