<!DOCTYPE html>
<html>
  <head>
    <title><%= meta_title %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <!-- SEO Meta Tags -->
    <meta name="description" content="<%= meta_description %>">
    <meta name="keywords" content="FPL, Fantasy Premier League, FPL rankings, FPL player rankings, fantasy football, FPL consensus, FPL transfer advice, FPL captains, FPL gameweek rankings">
    <link rel="canonical" href="<%= meta_url %>">

    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="<%= meta_title %>">
    <meta property="og:description" content="<%= meta_description %>">
    <meta property="og:image" content="<%= meta_image %>">
    <meta property="og:url" content="<%= meta_url %>">
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="Fantasy Forecast">

    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= meta_title %>">
    <meta name="twitter:description" content="<%= meta_description %>">
    <meta name="twitter:image" content="<%= meta_image %>">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= render 'shared/google_analytics' %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="shortcut icon" href="/favicon.ico">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="bg-white text-gray-800 p-4 border-b border-gray-200 fixed top-0 left-0 right-0 z-50 shadow-md" data-controller="mobile-menu">
      <div class="container mx-auto">
        <div class="flex justify-between items-center">
          <!-- Logo -->
          <div class="flex items-center">
            <%= link_to "⚡ Fantasy Forecast", root_path, class: "text-lg md:text-xl font-bold" %>
          </div>

          <!-- Desktop Navigation -->
          <div class="hidden md:flex items-center space-x-6">
            <%= link_to "Player Rankings", players_path, class: "text-gray-600 hover:text-gray-800 #{'font-semibold text-gray-800' if controller_name == 'players'}" %>
            <%= link_to "Forecaster Rankings", forecasters_path, class: "text-gray-600 hover:text-gray-800 #{'font-semibold text-gray-800' if controller_name == 'forecasters'}" %>
          </div>

          <!-- Desktop Auth Links -->
          <div class="hidden md:flex space-x-4">
            <% if user_signed_in? %>
              <%= link_to "My Rankings", forecaster_path(current_user), class: "text-gray-600 hover:text-gray-800" %>
              <%= link_to "My Account", edit_user_registration_path, class: "text-gray-600 hover:text-gray-800" %>
              <%= link_to "Logout", destroy_user_session_path, data: { "turbo-method": :delete }, class: "text-gray-600 hover:text-gray-800" %>
            <% else %>
              <%= link_to "Login", new_user_session_path, class: "border border-gray-800 text-gray-800 hover:bg-gray-100 px-3 py-1 rounded" %>
              <%= link_to "Sign Up", new_user_registration_path, class: "bg-amber-200 hover:bg-amber-300 text-gray-800 px-3 py-1 rounded font-semibold" %>
            <% end %>
          </div>

          <!-- Mobile Burger Button -->
          <button type="button" class="md:hidden text-gray-800 focus:outline-none" data-action="click->mobile-menu#toggle">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>

        <!-- Mobile Menu -->
        <div class="hidden md:hidden mt-4 pb-4 border-t border-gray-200 pt-4" data-mobile-menu-target="menu">
          <div class="flex flex-col space-y-3">
            <%= link_to "Player Rankings", players_path, class: "text-gray-600 hover:text-gray-800 py-2 #{'font-semibold text-gray-800' if controller_name == 'players'}", data: { action: "click->mobile-menu#close" } %>
            <%= link_to "Forecaster Rankings", forecasters_path, class: "text-gray-600 hover:text-gray-800 py-2 #{'font-semibold text-gray-800' if controller_name == 'forecasters'}", data: { action: "click->mobile-menu#close" } %>

            <div class="border-t border-gray-200 pt-3 mt-3">
              <% if user_signed_in? %>
                <%= link_to "My Rankings", forecaster_path(current_user), class: "block text-gray-600 hover:text-gray-800 py-2", data: { action: "click->mobile-menu#close" } %>
                <%= link_to "My Account", edit_user_registration_path, class: "block text-gray-600 hover:text-gray-800 py-2", data: { action: "click->mobile-menu#close" } %>
                <%= link_to "Logout", destroy_user_session_path, data: { "turbo-method": :delete, action: "click->mobile-menu#close" }, class: "block border border-gray-800 text-gray-800 hover:bg-gray-100 px-3 py-2 rounded text-center mt-2" %>
              <% else %>
                <%= link_to "Login", new_user_session_path, class: "block border border-gray-800 text-gray-800 hover:bg-gray-100 px-3 py-2 rounded text-center mb-2", data: { action: "click->mobile-menu#close" } %>
                <%= link_to "Sign Up", new_user_registration_path, class: "block bg-amber-200 hover:bg-amber-300 text-gray-800 font-semibold px-3 py-2 rounded text-center", data: { action: "click->mobile-menu#close" } %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </nav>

    <div class="pt-20">
      <% if notice %>
        <div class="bg-amber-100 border border-amber-400 text-amber-800 px-4 py-3 rounded mb-4 mx-4 mt-4">
          <%= notice %>
        </div>
      <% end %>

      <% if alert %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4 mx-4 mt-4">
          <%= alert %>
        </div>
      <% end %>
    </div>

    <main class="container mx-auto mt-4 md:mt-8 px-2 md:px-5">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white py-8 md:py-12 mt-12 md:mt-16">
      <div class="container mx-auto px-4">
        <div class="grid md:grid-cols-4 gap-6 md:gap-8">
          <div>
            <h3 class="text-lg md:text-xl font-bold mb-3 md:mb-4">⚡ Fantasy Forecast</h3>
            <p class="text-sm md:text-base text-gray-300">
              The most accurate FPL consensus weekly rankings from experienced Fantasy Premier League managers.
            </p>
          </div>
          <div>
            <ul class="space-y-2 text-sm md:text-base text-gray-300">
              <li><%= link_to "Privacy Policy", privacy_policy_path, class: "hover:text-white" %></li>
              <li><%= link_to "Terms of Service", terms_of_service_path, class: "hover:text-white" %></li>
              <li><%= link_to "Cookie Policy", cookie_policy_path, class: "hover:text-white" %></li>
              <li><%= link_to "Contact Us", contact_us_path, class: "hover:text-white" %></li>
            </ul>
          </div>
        </div>
        <div class="border-t border-gray-700 mt-6 md:mt-8 pt-6 md:pt-8 text-center text-gray-300">
          <p class="text-xs md:text-sm">&copy; 2025 Fantasy Forecast. All rights reserved. FPL consensus weekly rankings for Fantasy Premier League managers.</p>
          <p class="mt-2 text-xs">Not affiliated with the Premier League or Fantasy Premier League. All player and team names are trademarks of their respective owners.</p>
        </div>
      </div>
    </footer>
  </body>
</html>
